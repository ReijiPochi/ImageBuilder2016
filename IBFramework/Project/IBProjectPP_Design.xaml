<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:IBFramework.Project"
                    xmlns:ibgui="clr-namespace:IBGUI;assembly=IBGUI">

    <Style TargetType="{x:Type local:IBProjectPP}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:IBProjectPP}">
                    <Grid MinWidth="150" Margin="10,10,10,0">
                        <ibgui:IBFocusableBG/>
                        <StackPanel x:Name="Names" Width="100" HorizontalAlignment="Left">
                            <ibgui:IBLabel Content="Name"/>
                        </StackPanel>
                        <StackPanel x:Name="Values" Margin="100,0,0,0">
                            <ibgui:IBTextBox Text="{Binding IBProjectName}" Height="20" Padding="0" Margin="0,1,0,1"/>
                        </StackPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>