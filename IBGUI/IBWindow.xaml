<Window x:Name="window" x:Class="IBGUI.IBWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IBGUI"
        mc:Ignorable="d"
        Title="Image Builder" Height="300" Width="300" Style="{StaticResource IBWindowStyle}" Background="{DynamicResource IBBackGroundBrush}" Closing="window_Closing">
    <!--<Window.Resources>
        <ResourceDictionary Source="/IBGUI;component/ResourceDictionarys.xaml"/>
    </Window.Resources>-->

    <local:IBWorkspace Margin="0,28,0,0">
        <local:IBPanel>
            <local:IBTabControl x:Name="MainTabControl">

            </local:IBTabControl>
        </local:IBPanel>
    </local:IBWorkspace>
</Window>
